{
  "$schema": "https://turbo.build/schema.json",
  "globalDotEnv": [".env"],
  "pipeline": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "sdb:schema": {
      "inputs": ["src/schema/surreal/*.surql"]
    }
  }
}
